# Project images

Drop images in this folder using these exact filenames. They are referenced
from `src/components/projectdata.js`.

Anything missing just falls back to the coloured gradient behind it — no build
error, so you can add them one at a time.

| Filename             | Image                                                    |
| -------------------- | -------------------------------------------------------- |
| `dissertation.jpg`   | Object-detection screenshot (bounding boxes on the boats) |
| `hri.jpg`            | TIAGo robot at the table with the Lego towers             |
| `bus.jpg`            | Wall-mounted e-ink bus display                            |
| `tensoroom.jpg`      | _still needed_                                            |
| `iteration-inc.jpg`  | _still needed_                                            |
| `bonin-loot.jpg`     | _still needed_ (Bonin' Loot cover art / screenshot)       |
| `class-rep.jpg`      | _still needed_                                            |
| `hw-racing.jpg`      | _still needed_ (Heriot-Watt Racing car or team)           |

Landscape crops around 16:9 work best — the cards and slides use that ratio.
Keep them under ~300KB each so the page stays quick to load.
